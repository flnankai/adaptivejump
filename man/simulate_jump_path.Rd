% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulation.R
\name{simulate_jump_path}
\alias{simulate_jump_path}
\title{Simulate a stochastic-volatility jump path}
\usage{
simulate_jump_path(
  model = c("null", "dense", "sparse"),
  days = 1,
  seconds_per_day = 23400,
  sampling_seconds = 1,
  x0 = log(25),
  v0 = 0.16,
  beta_bar = 0.16,
  kappa = 5,
  gamma_vol = 0.5,
  rho = -0.5,
  theta = 1,
  lambda = 1,
  mark_sd = 1,
  mark_distribution = c("normal", "binary"),
  noise_sd = 0,
  noise_distribution = c("normal", "t8"),
  noise_model = c("iid", "ajl_gaussian"),
  seed = NULL
)
}
\arguments{
\item{model}{One of \code{"null"}, \code{"dense"}, or \code{"sparse"}.}

\item{days}{Number of trading days in the path.}

\item{seconds_per_day}{Length of a trading day in seconds.}

\item{sampling_seconds}{Sampling interval in seconds.}

\item{x0}{Initial log price.}

\item{v0}{Initial variance.}

\item{beta_bar}{Long-run variance.}

\item{kappa}{Variance mean-reversion coefficient.}

\item{gamma_vol}{Volatility of variance.}

\item{rho}{Correlation between price and variance Brownian motions.}

\item{theta}{Dense-alternative intensity coefficient.}

\item{lambda}{Sparse-alternative daily jump intensity.}

\item{mark_sd}{Standard deviation of jump marks.}

\item{mark_distribution}{Either \code{"normal"} or symmetric \code{"binary"}.}

\item{noise_sd}{Additive observation-noise standard deviation.  Set to zero
for the frictionless path.}

\item{noise_distribution}{Either \code{"normal"} or variance-standardized \code{"t8"}.}

\item{noise_model}{Either homoskedastic \code{"iid"} or \code{"ajl_gaussian"}, which
scales noise with instantaneous volatility.}

\item{seed}{Optional seed for a single direct simulation.}
}
\value{
A list containing latent and observed log prices, variance, jump
increments, and the normalized sampling interval.
}
\description{
Simulates the Heston-style log-price design used throughout the paper:
\deqn{dX_t = -V_t dt/2 + sqrt(V_t)dW_t + dJ_t,}
with a CIR-type variance process and leverage.  The dense alternative uses
a per-step Poisson intensity \code{theta * sqrt(delta)} and jump size
\code{sqrt(V) * sqrt(delta) * Y}; the sparse alternative uses a compound Poisson
process with daily intensity \code{lambda}.
}
